- hosts:
    - nfs

  vars:
    nfs_foldes_create: true
    nfs_foldes:
      - path: "/nfs/test"
        address: "192.168.1.0/24"
        permissions: "(rw,sync,no_subtree_check)"


  roles:
    - role: NFS
      become: true